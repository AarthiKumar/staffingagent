# Pytest configuration
[pytest]
testpaths = app/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Add current directory to Python path so 'app' module can be imported
pythonpath = .

# Show verbose output and shorter tracebacks
addopts = -v --tb=short
